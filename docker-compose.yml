services:
  relbot:
    build: .
    restart: unless-stopped
    env_file: .env  # copy .env.example to .env and fill in DISCORD_TOKEN first
    environment:
      DISCORD_TOKEN: ${DISCORD_TOKEN}
      DB_PATH: /data/relationships.db
    volumes:
      - relbot-data:/data

volumes:
  relbot-data:
